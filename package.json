{
  "name": "company",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:migrate": "pnpm --filter=packages/db migrate",
    "db:seed": "pnpm --filter=packages/db seed",
    "db:studio": "pnpm --filter=packages/db studio"
  },
  "devDependencies": {
    "turbo": "^2.8.17",
    "typescript": "^5.9.3",
    "prettier": "^3.8.1"
  },
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  }
}
